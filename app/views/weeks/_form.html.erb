<%= form_with(model: week, local: true) do |form| %>
  <% if week.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(week.errors.count, "error") %> prohibited this week from being saved:</h2>

      <ul>
        <% week.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date %>
  </div>

  <div class="field">
    <%= form.label :num_users_per_group %>
    <%= form.number_field :num_users_per_group %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
