<p id="notice"><%= notice %></p>

<h1>Weeks</h1>

<table>
  <thead>
    <tr>
      <th>Number</th>
      <th>Start date</th>
      <th>End date</th>
      <th>Num users per group</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @weeks.each do |week| %>
      <tr>
        <td><%= week.number %></td>
        <td><%= week.start_date %></td>
        <td><%= week.end_date %></td>
        <td><%= week.num_users_per_group %></td>
        <td><%= link_to 'Show', week %></td>
        <td><%= link_to 'Edit', edit_week_path(week) %></td>
        <td><%= link_to 'Destroy', week, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Week', new_week_path %>
